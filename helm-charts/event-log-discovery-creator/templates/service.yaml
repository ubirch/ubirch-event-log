apiVersion: v1
kind: Service
metadata:
  name: {{ include "event-log-discovery-creator.fullname" . }}

  labels:
    app.kubernetes.io/name: {{ include "event-log-discovery-creator.name" . }}
    helm.sh/chart: {{ include "event-log-discovery-creator.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: prometheus-metrics
      port: {{ .Values.eventLogDiscoveryCreator.prometheus.port}}
      protocol: TCP
    - name: jmx
      port: 9010
      protocol: TCP
    - name: debug
      port: 9020
      protocol: TCP

  selector:
    app.kubernetes.io/name: {{ include "event-log-discovery-creator.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
