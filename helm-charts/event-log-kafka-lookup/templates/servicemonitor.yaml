apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: event-log-kafka-lookup
  namespace: prometheus
  labels:
    app: event-log-kafka-lookup
    release: prometheus-operator
spec:
  endpoints:
  - interval: 30s
    path: /
    port: prometheus-metrics
  namespaceSelector:
    matchNames:
    - {{ .Release.Namespace }}
  selector:
    name: "event-log-kafka-lookup"
