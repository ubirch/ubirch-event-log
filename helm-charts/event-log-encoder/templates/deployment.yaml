apiVersion: apps/v1
kind: Deployment
metadata:
    name: {{ include "event-log-encoder.fullname" . }}
    labels:
        app.kubernetes.io/name: {{ include "event-log-encoder.name" . }}
        helm.sh/chart: {{ include "event-log-encoder.chart" . }}
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
    replicas: {{ .Values.eventLogEncoder.replicaCount }}
    selector:
        matchLabels:
            app.kubernetes.io/name: {{ include "event-log-encoder.name" . }}
            app.kubernetes.io/instance: {{ .Release.Name }}
    template:
        metadata:
            labels:
                app.kubernetes.io/name: {{ include "event-log-encoder.name" . }}
                app.kubernetes.io/instance: {{ .Release.Name }}
        spec:
            containers:
                - name: {{ .Chart.Name }}
                  image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
                  imagePullPolicy: {{ .Values.image.pullPolicy }}
                  ports:
                      - containerPort: 9010
                      - containerPort: 9020
                      - containerPort: 4321
                  readinessProbe:
                      tcpSocket:
                          port: 4321
                      initialDelaySeconds: 10
                      periodSeconds: 10
                  livenessProbe:
                      tcpSocket:
                          port: 9010
                      initialDelaySeconds: 20
                      periodSeconds: 60
                  env:
                      - name: UPA_KFC_NODES
                        value: "{{ .Values.eventLogEncoder.kafkaConsumer.bootstrapServers}}"
                      - name: UPA_KFC_TOPIC
                        value: "{{ .Values.eventLogEncoder.kafkaConsumer.topic}}"
                      - name: UPA_KFC_GROUP
                        value: "{{ .Values.eventLogEncoder.kafkaConsumer.groupId}}"
                      - name: UPA_KFP_NODES
                        value: "{{ .Values.eventLogEncoder.kafkaProducer.bootstrapServers}}"
                      - name: UPA_KFP_TOPIC
                        value: "{{ .Values.eventLogEncoder.kafkaProducer.topic}}"
                      - name: UPA_KFP_ERROR_TOPIC
                        value: "{{ .Values.eventLogEncoder.kafkaProducer.errorTopic}}"
                      - name: UPA_PROM_HTTP_PORT
                        value: "{{ .Values.eventLogEncoder.prometheus.port}}"
                      - name: UPA_KFC_MAX_POLL_RECORDS
                        value: "{{ .Values.eventLogEncoder.kafkaConsumer.maxpoll}}"
                  resources:
{{- toYaml .Values.resources | nindent 22 }}
                {{- with .Values.nodeSelector }}
            nodeSelector:
                {{- toYaml . | nindent 8 }}
            {{- end }}
            {{- with .Values.affinity }}
            affinity:
            {{- toYaml . | nindent 8 }}
            {{- end }}
            {{- with .Values.tolerations }}
            tolerations:
            {{- toYaml . | nindent 8 }}
        {{- end }}
